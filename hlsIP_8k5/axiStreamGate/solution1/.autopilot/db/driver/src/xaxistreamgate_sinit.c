// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xaxistreamgate.h"

extern XAxistreamgate_Config XAxistreamgate_ConfigTable[];

XAxistreamgate_Config *XAxistreamgate_LookupConfig(u16 DeviceId) {
	XAxistreamgate_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XAXISTREAMGATE_NUM_INSTANCES; Index++) {
		if (XAxistreamgate_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XAxistreamgate_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XAxistreamgate_Initialize(XAxistreamgate *InstancePtr, u16 DeviceId) {
	XAxistreamgate_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XAxistreamgate_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XAxistreamgate_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

