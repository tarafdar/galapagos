// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.3
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xminitor.h"

extern XMinitor_Config XMinitor_ConfigTable[];

XMinitor_Config *XMinitor_LookupConfig(u16 DeviceId) {
	XMinitor_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XMINITOR_NUM_INSTANCES; Index++) {
		if (XMinitor_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XMinitor_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XMinitor_Initialize(XMinitor *InstancePtr, u16 DeviceId) {
	XMinitor_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XMinitor_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XMinitor_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

